import 'dart:convert';

import 'package:flutter/material.dart';

import 'src/my_app.dart';

void main() {
  // var map = {"name": "ZTech", "birthYear": 2023};
  // var mapString = json.encode(map);
  // print(mapString);
  // var map2 = json.decode(mapString) as Map<String, dynamic>;

  runApp(const MyApp());
}
